@Aspect
@Component
public class JsonValidationAspect {

    @Before("@annotation(org.springframework.web.bind.annotation.RequestBody) && args(requestBody)")
    public void validateJson(Object requestBody) throws JsonInvalidException {
        // Perform JSON validation on requestBody
        if (!isValid(requestBody)) {
            throw new JsonInvalidException("Invalid JSON format");
        }
    }

    private boolean isValid(Object requestBody) {
        // Implement your JSON validation logic
        return true; // Placeholder
    }
}